# 刻迹浅色玻璃设计 QA · 2026-09-14

final result: passed

导航补充QA：用户确认将个人中心移至底部「我的」，报告移至今日右上角，并新增项目报告入口。浏览器实测「我的→隐私→返回」、今日报告→返回、Valley项目→报告（仅1个等待Plan、0已验收）→返回；项目报告截图布局完整。项目范围测试先观察2项失败再实现，现3项通过。旧图顶部头像/底部报告的差异为明确批准的导航调整，不是保真回归。

结论仅针对本地交互设计稿，不代表生产iOS、Valley、CLI已实现或经过测试。

## 证据与归一化

- Source visual truth: `/opt/coding/planb/github/.worktrees/timetrace-remote-runner/design/public/review-reference/selected-light.png`，用户原图853×1844。
- Implementation: http://127.0.0.1:5194/review 。今日、默认浅色、Joey演示账号、目标2/3、默认显示模块。
- Implementation screenshot: 本轮CUA的“核对小屏截图和运行错误”与“复核参考图与新版首屏”内联截图。浏览器未返回截图文件路径，故不伪造本地PNG路径。
- 同屏证据：http://127.0.0.1:5194/comparison.html 。900×930画布内并排展示参考和实时原型，各390×844 CSS px；参考缩放到同尺寸，约0.1%长宽比差。实现截图390×844像素，1:1 CSS输出，无设备边框或OS状态栏。
- 字段和按钮在同屏图及独立390px截图可读；另通过实际弹层、注册、主题与反馈页检查细节，因此无需另裁剪放大图。

## 比较与修复记录

1. 首轮P2：主视觉/模块垂直间距过大，雪山寄语在固定导航下被裁切；主图矩形边缘突兀。修复：调整标题、hero高度与负边距、模块间距；图片边缘mask渐隐；替换协作箭头为真实生成光带。
2. 修复后同尺寸再次捕获并排图：目标、人/AI切换、验收、工具、寄语与导航均可见，主区域顺序与比例恢复。无遗留P0/P1/P2视觉问题。
3. 功能P2：反馈回执随个人中心卸载丢失。修复：将tickets提升到工作台会话状态；复测提交FB-001→首页→个人中心→反馈，回执数量1。刷新仍清空，符合演示边界。

## 五项保真检查

| 项目 | 结果 |
| --- | --- |
| 字体 | 系统SF/PingFang回退，深蓝主标题与灰蓝辅助信息；层级、换行与参考接近。不是宣称获得了原图字体文件。 |
| 间距/布局 | 390×844首屏主要区域全部可见；固定底部导航不遮挡交互，卡片与圆角统一。 |
| 颜色 | 冰晶白、冰蓝强调、低饱和阴影；深色另测可读。状态同时有文字，不仅靠颜色。 |
| 图片 | 玻璃、人物头像、光带、雪山均为真实生成raster，已放入组件；图标用Phosphor。不是整张参考截图作为App界面。 |
| 内容 | 关键目标、2/3验收、我/AI、待处理、工具额度及零额外付费保留。“处理中”改为“结果待确认”以对应可验收状态。 |

## 交互验证

- 首页验收初始禁用，勾选三项才可提交，目标2/3→3/3，报告同步。
- 新项目→任务(P0)→Plan→执行→模拟完成→逐项验收；运行中工具选择锁定。
- 周额度耗尽时模拟恢复仍阻塞；取消周阻塞后才原会话续跑。
- 注册不勾选演示协议确认显示错误；勾选后完成假注册，不发送验证码或真实协议。
- 深色主题与减少动态效果可选，后恢复浅色；反馈回执跨导航保留。
- 本轮检查浏览器console error为空。
- 最终 `npm run lint`、`npm test`（2文件/9测试）、`npm run build` 和 `git diff --check` 均通过；build仅有966KB合并JS超过500KB提示。
- 390×844无横向溢出。尝试320×740但返回实际尺寸仍390×844，不计入320px通过；更窄屏、键盘、大字体、VoiceOver及原生性能待iOS阶段实测。

## P3后续打磨

- 生成主图不是原图逐像素复刻；装饰手写英文未复现，雪山裁切和光带形状略不同。
- 工具/按钮采用可访问图标库而非参考中的立体图标，主按钮宽度与标题光学字重可随review微调。
- PNG资产与新旧原型合并bundle较大，生产迁移前应优化图片尺寸/格式、按路由拆包；不阻塞本地预览。

## 交付检查

- [x] 选定参考与实时页面同屏比较，修复后重新捕获。
- [x] 主路径和新增账号相关演示状态通过浏览器操作验证。
- [x] 登录、权限、反馈及AI演示边界明确；无真实账户/CLI动作。
- [x] 更新iOS I4、Valley V6及总计划，等待用户review。
- [ ] 原生iOS及真实服务端联调、完整可访问性与性能测试：后续实施范围。
